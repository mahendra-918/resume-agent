[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn resume_agent.api:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
